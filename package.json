{
    "license": "AGPLv3",
    "private": true,
    "scripts": {
        "dev-server": "bin/console assets:install assets -vvv && encore dev-server",
        "dev": "bin/console assets:install assets -vvv && encore dev",
        "watch": "bin/console assets:install assets -vvv && encore dev --watch",
        "build": "bin/console assets:install assets -vvv && encore production --progress"
    },
    "browserslist": [
        "defaults"
    ],
    "devDependencies": {},
    "dependencies": {
    }
}
